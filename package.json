{
  "name": "heartbeat-monitor",
  "version": "1.0.0",
  "description": "Cron heartbeat: Prometheus CPU/memory + orders/chats counts per minute in a MongoDB time-series collection, with a dashboard API",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "seed": "node scripts/seed.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "mongoose": "^8.9.0",
    "node-cron": "^3.0.3"
  }
}
